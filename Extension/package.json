{
  "name": "vettavista-extension",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --dev",
    "watch": "node build.js --watch",
    "watch:dev": "node build.js --watch --dev"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.293",
    "chokidar": "^4.0.3",
    "esbuild": "^0.20.0",
    "fs-extra": "^11.3.1",
    "typescript": "^5.7.3"
  }
}
